process SIMPLE = {

  source = PoolSource {
    untracked vstring fileNames = {'file:pixel_tracks.root'}
    untracked int32 maxEvents = 100
  }

  module analysis = PixelTrackAnalysis{ 
    string TrackCollection = "PixelTracks" 
  }

  path p = {analysis}

}
