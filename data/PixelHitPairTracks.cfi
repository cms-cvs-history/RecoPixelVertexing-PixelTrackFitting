module pixelTracks = PixelHitPairTrackProducer{

  InputTag HitCollectionLabel = siPixelRecHits

  string Fitter = "PixelFitterByHelixProjections" 
  PSet   FitterPSet = {}

  string Filter = "PixelTrackFilterByKinematics"
  PSet   FilterPSet = { 
    double ptMin = 0.
    double tipMax = 1.0
    double chi2 = 1000.
  }
}
